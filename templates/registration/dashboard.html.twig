{% extends 'base.html.twig' %}

{% block title %}Dashboard{% endblock %}

{% block myContent %}

    <h1>Dashboard</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Name</th>
            <td>{{ duck.duckname }}</td>
            <td>{{ duck.firstname }}</td>
            <td>{{ duck.lastname }}</td>
        </tr>
        <tr>
            <th>Email</th>
            <td>{{ duck.email }}</td>
        </tr>

        </tbody>
    </table>

    <a href="{{ path('app_quack_index') }}">Back to home</a>

    <a href="{{ path('app_duck_edit') }}">Edit my account</a>

    {{ include('registration/_delete_duck.html.twig') }}
{% endblock %}
