{% extends 'base.html.twig' %}

{% block title %}Coincoin{% endblock %}

{% block myContent %}
    <h1>Coincoin</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ coincoin.id }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ coincoin.message }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>{{ coincoin.picture }}</td>
            </tr>
            <tr>
                <th>Created_time</th>
                <td>{{ coincoin.createdTime ? coincoin.createdTime|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_coincoin_by_quack', 'id': coincoin.parentId) }}">back to list</a>

    <a href="{{ path('app_coincoin_edit', {'id': coincoin.id}) }}">edit</a>

    {{ include('coincoin/_delete_form.html.twig') }}
{% endblock %}
