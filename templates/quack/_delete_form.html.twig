<form method="post" action="{{ path('app_quack_delete', {'id': quack.id}) }}" onsubmit="return confirm('Are you sure you want to delete this account?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ quack.id) }}">
    <button class="btn">Delete</button>
</form>
